import { UIType } from '../../../../../../Types/System'

const Layouts: React.FC<{ UI: UIType }> = ({ UI }) => {
  // Vars

  // Lifecycle

  // UI
  return (
    <UI.Card title="Layouts" animate>
      Layouts
    </UI.Card>
  )
}

export default Layouts
