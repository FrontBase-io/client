import { UIType } from '../../../../../../Types/System'

const Overviews: React.FC<{ UI: UIType }> = ({ UI }) => {
  // Vars

  // Lifecycle

  // UI
  return (
    <UI.Card title="Overviews" animate>
      Overviews
    </UI.Card>
  )
}

export default Overviews
