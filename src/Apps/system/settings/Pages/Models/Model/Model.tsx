import { UIType } from '../../../../../../Types/System'

const Model: React.FC<{ UI: UIType }> = ({ UI }) => {
  // Vars

  // Lifecycle

  // UI
  return (
    <UI.Card title="Models" animate>
      Models
    </UI.Card>
  )
}

export default Model
