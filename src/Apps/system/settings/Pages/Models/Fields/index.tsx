import { UIType } from '../../../../../../Types/System'

const Fields: React.FC<{ UI: UIType }> = ({ UI }) => {
  // Vars

  // Lifecycle

  // UI
  return (
    <UI.Card title="Fields" animate>
      Fields
    </UI.Card>
  )
}

export default Fields
