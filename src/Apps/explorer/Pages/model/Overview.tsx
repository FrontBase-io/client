import ModelOverview from '../../../../Components/App/Model/Overview'
import { ModelType } from '../../../../Types/Model'
import { PageProps } from '../../../Types'

const Overview: React.FC<PageProps> = ({ model }) => {
  // Vars

  // Lifecycle

  // Functions

  // UI
  return <ModelOverview model={model} />
}

export default Overview
