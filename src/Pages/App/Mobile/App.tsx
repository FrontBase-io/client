import { PageType } from '../../../Types/Apps'

const MobileApp: React.FC<{ pages: PageType[] }> = ({ pages }) => {
  // Vars

  // Lifecycle

  // UI
  return <>MobileApp</>
}

export default MobileApp
