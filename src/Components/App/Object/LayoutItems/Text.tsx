import { LayoutItemType } from '../LayoutItem'

const LIText: React.FC<LayoutItemType> = ({ layoutItem }) => {
  // Vars

  // Lifecycle

  // Functions

  // UI
  return <>{layoutItem.settings?.text}</>
}

export default LIText
